# Global instructions

Read `~/.agents/profile/PROFILE.md` for my background and preferences.

## Who I am

ML researcher (AI4Science / chemistry), part-time Python developer. I use Windows, Mac, remote Linux SSH, and switch between Claude Code, Cursor, and Codex.

## Before starting project work

1. Read `CONTEXT.md` in the project root (if present)
2. Read `AGENTS.md` in the project root (if present)
3. Check `git status` and recent commits

## Coding standards

- Python, type hints on public APIs
- Ruff for lint/format
- Small focused diffs; no unrelated changes
- Don't commit or push unless I ask
- Run commands yourself; retry with alternatives on failure
- Scientific writing: verifiable citations only

## Skills

Custom skills live in `~/.codex/skills/` (chemistry, ML, writing, Zotero, etc.). Prefer them over improvising workflows.

## Handoff

When I say I'm continuing from another tool, treat `CONTEXT.md` as the source of truth for session state.
